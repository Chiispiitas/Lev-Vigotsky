/* Speaking Report Maker - offline, mobile-focused static app */
const CLASS_DATA = [
  {
    "id": "decimo-c",
    "label": "10° Solidario C",
    "course": "DÉCIMO DE BÁSICA - SOLIDARIO",
    "section": "C",
    "specialty": "Educación General Básica",
    "tutor": "DAVID ALEJANDRO SANTANA MORALES",
    "source": "DECIMO C.pdf",
    "students": [
      {
        "n": 1,
        "name": "ALCIVAR MORA EFRAIN LEONARDO"
      },
      {
        "n": 2,
        "name": "ALVARADO LUCAS MAY ANAHI"
      },
      {
        "n": 3,
        "name": "ALVIA MENDOZA JORGE ISAIAS"
      },
      {
        "n": 4,
        "name": "BURGOS VÉLEZ ANTHONY JOSHUE"
      },
      {
        "n": 5,
        "name": "CALDERON SEGOVIA LUIS EDUARDO"
      },
      {
        "n": 6,
        "name": "CAVAL CUENCA ISAAC FABRICIO"
      },
      {
        "n": 7,
        "name": "CEDEÑO BILLIGUA MAYLETH SOLANGE"
      },
      {
        "n": 8,
        "name": "CEVALLOS TORRES JORGE MATHIUS"
      },
      {
        "n": 9,
        "name": "CHAVEZ SOLORZANO LEYZITH JENEVY"
      },
      {
        "n": 10,
        "name": "CONSTANTE INTRIAGO ELIAN ANDRES"
      },
      {
        "n": 11,
        "name": "FIGUEROA MENDOZA MIA VALENTINA"
      },
      {
        "n": 12,
        "name": "LOOR SUAREZ KIARA VALENTINA"
      },
      {
        "n": 13,
        "name": "LUCAS VERA SANTIAGO JAVIER"
      },
      {
        "n": 14,
        "name": "MEDRANDA VERA AMBAR GIANELLA"
      },
      {
        "n": 15,
        "name": "MENDOZA FLORES JHON ROBERTH"
      },
      {
        "n": 16,
        "name": "MENDOZA LÓPEZ REBECA ANTHONELLA"
      },
      {
        "n": 17,
        "name": "MENDOZA PEÑAFIEL NATHALIA MISHELL"
      },
      {
        "n": 18,
        "name": "MOLINA MONTESDEOCA LEILANY ANAHI"
      },
      {
        "n": 19,
        "name": "MOTATO REVELO EMY LISSETH"
      },
      {
        "n": 20,
        "name": "PAREDES CHONG GABRIELA VALENTINA"
      },
      {
        "n": 21,
        "name": "PEÑARRIETA MURILLO JOEL EMILIANO"
      },
      {
        "n": 22,
        "name": "PINCAY ARTEAGA IKER DAMIAN"
      },
      {
        "n": 23,
        "name": "SOLORZANO FRANCO JESUS MIGUEL"
      },
      {
        "n": 24,
        "name": "VELEZ MARIN SEBASTIAN SAID"
      },
      {
        "n": 25,
        "name": "VERA ALAVA LYA MAYTE"
      },
      {
        "n": 26,
        "name": "ZAMBRANO LINO ALAN ALEXANDER"
      }
    ]
  },
  {
    "id": "primero-cc",
    "label": "1° BGU Ciencias A",
    "course": "PRIMERO DE BACHILLERATO",
    "section": "A",
    "specialty": "CIENCIAS",
    "tutor": "CARLOS EDUARDO MENDOZA TUBAY",
    "source": "PRIMERO CC.pdf",
    "students": [
      {
        "n": 1,
        "name": "AGUAYO PINCAY ELKIN MATEO"
      },
      {
        "n": 2,
        "name": "CAMPUZANO MURILLO LIA MARICELY"
      },
      {
        "n": 3,
        "name": "CATAGUA LOPEZ EDWIN MATIAS"
      },
      {
        "n": 4,
        "name": "CEDEÑO MENDOZA JEAN POOL"
      },
      {
        "n": 5,
        "name": "CEDEÑO MURILLO NAYESKA NAARA"
      },
      {
        "n": 6,
        "name": "CUENCA CEDEÑO ASAT JAVIER"
      },
      {
        "n": 7,
        "name": "DE JESUS GARCIA YUAN YARETH"
      },
      {
        "n": 8,
        "name": "DELGADO QUIJIJE ARTURO JOSEBA"
      },
      {
        "n": 9,
        "name": "GARCIA LUCAS NEY JOSUE"
      },
      {
        "n": 10,
        "name": "HOLGUIN VEINTIMILLA ALISS BRIGETTE"
      },
      {
        "n": 11,
        "name": "LANDAZURI VILELA JHON LUIGGER"
      },
      {
        "n": 12,
        "name": "LOOR LOOR CARLOS ALFREDO"
      },
      {
        "n": 13,
        "name": "LOPEZ PAZ MIRELY LISSETH"
      },
      {
        "n": 14,
        "name": "LORENTI MENDOZA JOAN MATHIAS"
      },
      {
        "n": 15,
        "name": "LOZA ZAMBRANO JOSEPH AARÓN"
      },
      {
        "n": 16,
        "name": "MACIAS ARTEAGA CRISTOPHER ARIEL"
      },
      {
        "n": 17,
        "name": "MACIAS MEZA ISAAC RAFAEL"
      },
      {
        "n": 18,
        "name": "MACIAS VERA ARIANA VALENTINA"
      },
      {
        "n": 19,
        "name": "MENDOZA TEJENA CRISTHIAN ALEJANDRO"
      },
      {
        "n": 20,
        "name": "MUÑOZ SUAREZ REBECA PAULINA"
      },
      {
        "n": 21,
        "name": "ORTEGA BELLO JEREMY MATEO"
      },
      {
        "n": 22,
        "name": "PALMA ALCIVAR KASLEY NOELY"
      },
      {
        "n": 23,
        "name": "PALMA SORNOZA XAVIER DAVID"
      },
      {
        "n": 24,
        "name": "PEÑAFIEL BARROS DARLA"
      },
      {
        "n": 25,
        "name": "QUIJIJE CATAGUA RUT JONAHIRA"
      },
      {
        "n": 26,
        "name": "SANCHEZ VALVERDE BRITHANY ANDREA"
      },
      {
        "n": 27,
        "name": "TAPIA TUBAY SCARLETH ALEJANDRA"
      },
      {
        "n": 28,
        "name": "TAQUES VEGA DYLAN SAID"
      },
      {
        "n": 29,
        "name": "VÉLIZ ROSADO MILEY LISBETH"
      },
      {
        "n": 30,
        "name": "VINCES GARCIA FRANSHESKA VALENTINA"
      },
      {
        "n": 31,
        "name": "VIZUETA GARCIA VALERIA ANDREINA"
      },
      {
        "n": 32,
        "name": "ZACASARI LÓPEZ LILLY GUADALUPE"
      },
      {
        "n": 33,
        "name": "ZAMBRANO ZAMBRANO ANGELA DAIMAY"
      },
      {
        "n": 34,
        "name": "ZAMORA ESMERALDAS MIRELY ANAHI"
      }
    ]
  },
  {
    "id": "primero-tec",
    "label": "1° BGU Técnico B",
    "course": "PRIMERO DE BACHILLERATO",
    "section": "B",
    "specialty": "TÉCNICO - CONTABILIDAD Y ADMINISTRACIÓN",
    "tutor": "JAHAIRA MARIUXI RODRÍGUEZ SALAZAR",
    "source": "PRIMERO TEC.pdf",
    "students": [
      {
        "n": 1,
        "name": "ALAVA LOOR JORDY ALEXANDER"
      },
      {
        "n": 2,
        "name": "BELLO MERO JUAN ANDRE"
      },
      {
        "n": 3,
        "name": "BRAVO ARAGUNDI NATHALIE BELEN"
      },
      {
        "n": 4,
        "name": "CEDEÑO LITARDO LUIS ZAID"
      },
      {
        "n": 5,
        "name": "CEDEÑO PICO MOISES DANIEL"
      },
      {
        "n": 6,
        "name": "CEVALLOS COBEÑA CAMILA ELIZABETH"
      },
      {
        "n": 7,
        "name": "CHAVEZ RIVERA CLARISSA DAYANNA"
      },
      {
        "n": 8,
        "name": "DELGADO CEDEÑO FERNANDO DIDDIER"
      },
      {
        "n": 9,
        "name": "DELGADO MOREIRA MIGUEL SAMAEL"
      },
      {
        "n": 10,
        "name": "DIAZ CARDENAS IRAE VALENTINA"
      },
      {
        "n": 11,
        "name": "FLORES PARRAGA ESTEFANIA CRISTHINA"
      },
      {
        "n": 12,
        "name": "GARCES VILLAVICENCIO MATHIUS KENY"
      },
      {
        "n": 13,
        "name": "GARCIA ACOSTA CAMILA SARAHI"
      },
      {
        "n": 14,
        "name": "IÑIGUEZ MOLINA MARTIN ALEJANDRO"
      },
      {
        "n": 15,
        "name": "LEON CUESTA ADRIANA CAMILA"
      },
      {
        "n": 16,
        "name": "LOOR VARGAS ROQUE ANDRE"
      },
      {
        "n": 17,
        "name": "MACÍAS BRAVO ITHANS ANDRÈ"
      },
      {
        "n": 18,
        "name": "MACIAS SORNOZA VAMPERSY DAMIAN"
      },
      {
        "n": 19,
        "name": "MACIAS VITE IAN ROLANDO"
      },
      {
        "n": 20,
        "name": "MEJIA CAÑIZARES RONELIX JOSUE"
      },
      {
        "n": 21,
        "name": "MORALES RIVERA AXEL ANDRE"
      },
      {
        "n": 22,
        "name": "ORELLANA CANTOS CAMILA VALENTINA"
      },
      {
        "n": 23,
        "name": "PAZMIÑO BAZURTO ZULEIKA KARELIZ"
      },
      {
        "n": 24,
        "name": "PRADO QUIROZ WILTON JAVIER"
      },
      {
        "n": 25,
        "name": "RÍOS VÉLEZ ASHLEY ROSSMERY"
      },
      {
        "n": 26,
        "name": "RIVERA LOOR IAM SNAYDER"
      },
      {
        "n": 27,
        "name": "RODRIGUEZ URDANIGO CESAR STEVEN"
      },
      {
        "n": 28,
        "name": "SALAZAR DELGADO KARLA KAREL"
      },
      {
        "n": 29,
        "name": "SANCHEZ GARCIA ISAAC ALEJANDRO"
      },
      {
        "n": 30,
        "name": "SOLORZANO BATALLAS JEIKO ANTONIO"
      },
      {
        "n": 31,
        "name": "VELÁSQUEZ VERA ARIEL ISAÍ"
      },
      {
        "n": 32,
        "name": "VELEZ GARCIA MAILYN AYLIN"
      },
      {
        "n": 33,
        "name": "VERA HIDALGO JACOBO EMILIO"
      },
      {
        "n": 34,
        "name": "ZAMBRANO CASTRO DARWIN JAVIER"
      },
      {
        "n": 35,
        "name": "ZAMBRANO COBEÑA JUNINHO GEOVANNY"
      },
      {
        "n": 36,
        "name": "ZAMBRANO ZAMBRANO NEICER YONAIKER"
      },
      {
        "n": 37,
        "name": "ZAMORA ZAMBRANO DANNA SARAI"
      }
    ]
  },
  {
    "id": "segundo-cc",
    "label": "2° BGU Ciencias A",
    "course": "SEGUNDO DE BACHILLERATO",
    "section": "A",
    "specialty": "CIENCIAS",
    "tutor": "Edison David Palma Palacios",
    "source": "SEGUNDO CC.pdf",
    "students": [
      {
        "n": 1,
        "name": "AGREDO BRICEÑO DARLY ANAHI"
      },
      {
        "n": 2,
        "name": "AGUAYO RAMÍREZ PAÚL IGNACIO"
      },
      {
        "n": 3,
        "name": "ALARCÓN ZAMBRANO GENESIS HERMIONE"
      },
      {
        "n": 4,
        "name": "ALCÍVAR MORA VICTORIA ISABELLA"
      },
      {
        "n": 5,
        "name": "ALVAREZ BRAVO ASHLEY RAPHAELA"
      },
      {
        "n": 6,
        "name": "BALAREZO ZAMBRANO CARLOS IVAN"
      },
      {
        "n": 7,
        "name": "BRAVO PAZMIÑO RENATA ELIZABETH"
      },
      {
        "n": 8,
        "name": "CEDEÑO DELGADO MILAGRO AUXILIADORA"
      },
      {
        "n": 9,
        "name": "CEDEÑO VELEZ ALINA TERESA"
      },
      {
        "n": 10,
        "name": "COOS ALAVA YOVANNY MATHYAS"
      },
      {
        "n": 11,
        "name": "COROZO BRITO DENNIS JOSUÉ"
      },
      {
        "n": 12,
        "name": "ESTRADA PATA JONAIKER AIL"
      },
      {
        "n": 13,
        "name": "GARCÉS ESPINALES MÍA MAYTTÉ"
      },
      {
        "n": 14,
        "name": "GARCÍA MACÍAS ARIANA NOHELIA"
      },
      {
        "n": 15,
        "name": "INTRIAGO MOREIRA GABRIEL ISAÍAS"
      },
      {
        "n": 16,
        "name": "JACOME MIELES CRISTINA ANALY"
      },
      {
        "n": 17,
        "name": "MACIAS GUERRERO KRUSPKAYA LOREDANA"
      },
      {
        "n": 18,
        "name": "MACIAS VINUEZA JUNAIRY SOLANGE"
      },
      {
        "n": 19,
        "name": "MAJOJO MERA DULCE MARIA"
      },
      {
        "n": 20,
        "name": "MEJIA RAMIREZ CALEB ISAAC"
      },
      {
        "n": 21,
        "name": "MITE ZAMORA HEYLIN VALENTINA"
      },
      {
        "n": 22,
        "name": "MOREIRA LITARDO JEFFREY JAHIR"
      },
      {
        "n": 23,
        "name": "MOREIRA MACÍAS NOHELIA ASTRID"
      },
      {
        "n": 24,
        "name": "ORDOÑEZ LOPEZ NAHOMY ADAMARYS"
      },
      {
        "n": 25,
        "name": "PALMA ALCIVAR KENYA NAIROBY"
      },
      {
        "n": 26,
        "name": "PARRAGA SANDOYA JUAN JOSE"
      },
      {
        "n": 27,
        "name": "PONCE BAQUE VERÓNICA XASBEIDY"
      },
      {
        "n": 28,
        "name": "QUIROZ ZAMBRANO CARLOS SEBASTIÁN"
      },
      {
        "n": 29,
        "name": "RODAS VALERIANO MATIAS ALEXANDER"
      },
      {
        "n": 30,
        "name": "SOLORZANO FRANCO ASHLEY DAYANA"
      },
      {
        "n": 31,
        "name": "SOTO FRANCO IAN SEBASTIÁN"
      },
      {
        "n": 32,
        "name": "VÉLEZ AYALA HELEN ALEJANDRA"
      },
      {
        "n": 33,
        "name": "VÉLEZ GUTIÉRREZ JOSÉ ADRIAN"
      },
      {
        "n": 34,
        "name": "VELEZ VERA KLEYRE LISBETH"
      },
      {
        "n": 35,
        "name": "VERA FRANCO ANDY XAVIER"
      },
      {
        "n": 36,
        "name": "VITERI PINCAY ALEX DANIEL"
      },
      {
        "n": 37,
        "name": "ZAMBRANO LINO LIZ ADRIANA"
      },
      {
        "n": 38,
        "name": "ZAMBRANO MOREIRA MAIRO GERARD"
      },
      {
        "n": 39,
        "name": "ZAMORA GUALPA MATIAS ALEJANDRO"
      }
    ]
  },
  {
    "id": "segundo-tec",
    "label": "2° BGU Técnico B",
    "course": "SEGUNDO DE BACHILLERATO",
    "section": "B",
    "specialty": "TÉCNICO - CONTABILIDAD Y ADMINISTRACIÓN",
    "tutor": "OSCAR OMAR ALCIVAR VALDERRAMO",
    "source": "SEGUNDO TEC.pdf",
    "students": [
      {
        "n": 1,
        "name": "ANDRADE PEÑARRIETA KLEINER MATIAS"
      },
      {
        "n": 2,
        "name": "ARTEAGA SOLÓRZANO JOSTYN ARIEL"
      },
      {
        "n": 3,
        "name": "BAZÁN MÉNDEZ JACKCOBY JEREMÍAS"
      },
      {
        "n": 4,
        "name": "BEJARANO DELGADO FABIAN MAXIMILIANO"
      },
      {
        "n": 5,
        "name": "CANTOS SALTOS JASU DIVINA"
      },
      {
        "n": 6,
        "name": "CHAVARRÍA BERMELLO ANGÉLICA MARÍA"
      },
      {
        "n": 7,
        "name": "LOOR SALAZAR DANIEL STEVEN"
      },
      {
        "n": 8,
        "name": "MENDOZA FLORES FREDERID JOSNAYDER"
      },
      {
        "n": 9,
        "name": "MERO ZAMBRANO JUAN SEBASTIÁN"
      },
      {
        "n": 10,
        "name": "MEZA DELGADO KRISTINE LEONELA"
      },
      {
        "n": 11,
        "name": "MOREIRA VERA PAUL JOSHUE"
      },
      {
        "n": 12,
        "name": "PALACIOS QUIROZ STEPHANO ADRIAN"
      },
      {
        "n": 13,
        "name": "RODRIGUEZ ZAVALA JOSE VICENTE"
      },
      {
        "n": 14,
        "name": "ROLDÁN SÁNCHEZ MAHOLY JASLIN"
      },
      {
        "n": 15,
        "name": "RUEDA GARCÍA CARIM ALEJANDRO"
      },
      {
        "n": 16,
        "name": "SAAVEDRA VERA AYMARA RAFAELA"
      },
      {
        "n": 17,
        "name": "VALENCIA MENDOZA JORDAN ARIEL"
      },
      {
        "n": 18,
        "name": "VASQUEZ CHAVEZ WESLEY JASSET"
      },
      {
        "n": 19,
        "name": "VELEZ LOPEZ TIFANY ANAHIS"
      },
      {
        "n": 20,
        "name": "VINCES MACIAS LEAH VALENTINA"
      },
      {
        "n": 21,
        "name": "ZAMBRANO MENENDEZ MAYKEL ADONYS"
      },
      {
        "n": 22,
        "name": "ZAMBRANO ZAMBRANO ADONYS JESUS"
      }
    ]
  },
  {
    "id": "tercero-cc",
    "label": "3° BGU Ciencias A",
    "course": "TERCERO DE BACHILLERATO",
    "section": "A",
    "specialty": "CIENCIAS",
    "tutor": "ISRAEL SMITH FRANCO MERO",
    "source": "TERCERO CC.pdf",
    "students": [
      {
        "n": 1,
        "name": "BARREIRO CARRANZA ALISSON YERINA"
      },
      {
        "n": 2,
        "name": "BASURTO SANTACRUZ TAMARA MICHELLE"
      },
      {
        "n": 3,
        "name": "BRIONES PLAZA FABRICIO SAMUEL"
      },
      {
        "n": 4,
        "name": "CEVALLOS MACIAS MADELINE JOSHENKA"
      },
      {
        "n": 5,
        "name": "DELGADO MENDOZA GIA ISABELLA"
      },
      {
        "n": 6,
        "name": "DELGADO PONCE ANA RAQUEL"
      },
      {
        "n": 7,
        "name": "ECHEVERRIA QUIROZ JOHAN JOSUE"
      },
      {
        "n": 8,
        "name": "FALCONEZ PALMA SHELY MADELYNE"
      },
      {
        "n": 9,
        "name": "FRANCO HIDALGO ETHAN ALFONSO"
      },
      {
        "n": 10,
        "name": "GONZALEZ MITE IVAN ISMAEL"
      },
      {
        "n": 11,
        "name": "JIMBO MACIAS EMILY ANAHI"
      },
      {
        "n": 12,
        "name": "LOOR BRIONES CRISTOPHER ALEJANDRO"
      },
      {
        "n": 13,
        "name": "LOOR GONZÁLEZ OSCAR ADRIEL"
      },
      {
        "n": 14,
        "name": "LUCAS VERA MARIS ALEJANDRA"
      },
      {
        "n": 15,
        "name": "LUZARDO RIVERA JERINA VALENTINA"
      },
      {
        "n": 16,
        "name": "MACIAS DEMERA MAHOLY DAYANARA"
      },
      {
        "n": 17,
        "name": "MACIAS VINCES EDUARDO IGNACIO"
      },
      {
        "n": 18,
        "name": "MEJIA ROMERO SHEYLA GABRIELA"
      },
      {
        "n": 19,
        "name": "MENENDEZ DELGADO MICHELLE VALENTINA"
      },
      {
        "n": 20,
        "name": "MERA SABANDO FERNANDO FABIAN"
      },
      {
        "n": 21,
        "name": "MERCHAN PIGUAVE EDGAR ALEXANDER"
      },
      {
        "n": 22,
        "name": "MONSERRATE SABANDO LINDSAY ANABELLA"
      },
      {
        "n": 23,
        "name": "MOREIRA ZAMBRANO MAYLEEN GENITH"
      },
      {
        "n": 24,
        "name": "MOTATO REVELO VALERY NICOLE"
      },
      {
        "n": 25,
        "name": "ORMAZA ZAMORA BIANCA MARÍA"
      },
      {
        "n": 26,
        "name": "PALACIOS MANZABA DANIELA ALEJANDRA"
      },
      {
        "n": 27,
        "name": "PALMA TREJO RICARDO ANDRES"
      },
      {
        "n": 28,
        "name": "PATIÑO ROLDÁN MAITE CAMILA"
      },
      {
        "n": 29,
        "name": "PEÑAFIEL PONCE ANGELES VICTORIA"
      },
      {
        "n": 30,
        "name": "PESANTES MACIAS MARIA CATALINA"
      },
      {
        "n": 31,
        "name": "PINELA ALCIVAR BRUNO DAMIAN"
      },
      {
        "n": 32,
        "name": "ROMERO SANTANA KEIRA ANALÍA"
      },
      {
        "n": 33,
        "name": "SALAZAR OÑATE CRISTIAN DAMIAN"
      },
      {
        "n": 34,
        "name": "SALDARRIAGA PADILLA DANIEL ALEJANDRO"
      },
      {
        "n": 35,
        "name": "SÁNCHEZ CARRERA JUAN PEDRO"
      },
      {
        "n": 36,
        "name": "VASQUES JIMENEZ IVANNA MELINA"
      },
      {
        "n": 37,
        "name": "VELIZ OCHOA ANGELINE JULIETH"
      },
      {
        "n": 38,
        "name": "ZAMBRANO GARCIA RIHANNA THAIS"
      }
    ]
  },
  {
    "id": "tercero-tec",
    "label": "3° BGU Técnico B",
    "course": "TERCERO DE BACHILLERATO TÉCNICO",
    "section": "B",
    "specialty": "TÉCNICO - CONTABILIDAD Y ADMINISTRACIÓN",
    "tutor": "VERÓNICA RAQUEL LÓPEZ FIGUEROA",
    "source": "TERCERO TEC.pdf",
    "students": [
      {
        "n": 1,
        "name": "ALCÍVAR CEDEÑO GÉNESSIS ANALÍA"
      },
      {
        "n": 2,
        "name": "ANDRADE LARA MATIAS SEBASTIAN"
      },
      {
        "n": 3,
        "name": "BAILÓN ESTRADA DILLAN SANTIAGO"
      },
      {
        "n": 4,
        "name": "CAÑARTE CASTRO FREDDY MATHIAS"
      },
      {
        "n": 5,
        "name": "CARREÑO MENDOZA KEILA ANAHÍ"
      },
      {
        "n": 6,
        "name": "CASTRELLON VERA ISMAEL"
      },
      {
        "n": 7,
        "name": "CEDEÑO QUIJIJE MATEO"
      },
      {
        "n": 8,
        "name": "CEDEÑO VASQUEZ MARCO ANDRES"
      },
      {
        "n": 9,
        "name": "CEVALLOS COBEÑA JOSE MATIAS"
      },
      {
        "n": 10,
        "name": "CEVALLOS LOPEZ SHIRLEY VALERIA"
      },
      {
        "n": 11,
        "name": "CEVALLOS YOSA EMILY SCARLETH"
      },
      {
        "n": 12,
        "name": "DELGADO MERO MAYKEL DARIAN"
      },
      {
        "n": 13,
        "name": "DUARTE AVEIGA CRISTHIAN VALENTIN"
      },
      {
        "n": 14,
        "name": "FALCONI MERO VIELKA ROMINA"
      },
      {
        "n": 15,
        "name": "FRANCO FRANCO LUISANA VALENTINA"
      },
      {
        "n": 16,
        "name": "LOOR OCHOA JOHAN PAUL"
      },
      {
        "n": 17,
        "name": "MACIAS TUBAY ANGELA DAMARYS"
      },
      {
        "n": 18,
        "name": "MASAPANTA ZAMBRANO JEAN MARCOS"
      },
      {
        "n": 19,
        "name": "MEDRANDA PONCE MELANY YANOSKA"
      },
      {
        "n": 20,
        "name": "MEJIA LOPEZ GERALD JOHAN"
      },
      {
        "n": 21,
        "name": "MENDOZA CAJAMARCA HANNA MICHELLE"
      },
      {
        "n": 22,
        "name": "MORRILLO CORDERO MIGUEL ANGEL"
      },
      {
        "n": 23,
        "name": "MUÑOZ SUAREZ BRANDON DARIO"
      },
      {
        "n": 24,
        "name": "NAULA SÁNCHEZ NOHELIA VALEZKA"
      },
      {
        "n": 25,
        "name": "PELAEZ SABANDO KEYLA DAMARIS"
      },
      {
        "n": 26,
        "name": "PIGUAVE BARREZUETA ANGIE NAYIBETH"
      },
      {
        "n": 27,
        "name": "REYES MERA RANDY ELIAN"
      },
      {
        "n": 28,
        "name": "REYES SABANDO BRITHANY PAMELA"
      },
      {
        "n": 29,
        "name": "RODRÍGUEZ URDÁNIGO BRUNO ABDÓN"
      },
      {
        "n": 30,
        "name": "SANTANA MEJIA SARA NAHOMY"
      },
      {
        "n": 31,
        "name": "TUMBACO MACÍAS JUAN DAVID"
      },
      {
        "n": 32,
        "name": "VELASCO REYES MILAGROS ANALIA"
      },
      {
        "n": 33,
        "name": "VERA MOREIRA BIANKA MILENA"
      },
      {
        "n": 34,
        "name": "VIDAL FREYA MELINA"
      },
      {
        "n": 35,
        "name": "ZAMORA ESCALANTE MATIAS BENJAMIN"
      }
    ]
  }
];

const RUBRIC = [
  {
    id: "englishOnly",
    title: "1. Comunicación solo en inglés",
    max: 1.5,
    options: [
      { label: "Excelente", points: 1.5, description: "Usa inglés durante toda la actividad. No usa español." },
      { label: "Bueno", points: 1.0, description: "Usa inglés casi todo el tiempo. Puede aparecer una palabra aislada en español." },
      { label: "Básico", points: 0.5, description: "Usa varias palabras en español. El mensaje depende parcialmente del español." },
      { label: "En proceso", points: 0, description: "Usa español frecuentemente o responde mayormente en español." }
    ]
  },
  {
    id: "spontaneous",
    title: "2. Sin lectura / producción espontánea",
    max: 1.5,
    options: [
      { label: "Excelente", points: 1.5, description: "Habla a partir de ideas. No lee. Solo usa palabras clave si fueron autorizadas." },
      { label: "Bueno", points: 1.0, description: "Habla mayormente de forma independiente. Puede mirar palabras clave, pero no lee oraciones completas." },
      { label: "Básico", points: 0.5, description: "Lee partes de la respuesta o depende mucho de sus notas." },
      { label: "En proceso", points: 0, description: "Lee un guion completo, usa celular/traductor o no puede continuar sin leer." }
    ]
  },
  {
    id: "taskCoverage",
    title: "3. Cobertura completa de preguntas y tarea",
    max: 2,
    options: [
      { label: "Excelente", points: 2.0, description: "Responde todo de forma completa, con detalles, razones, ejemplos y seguimiento." },
      { label: "Bueno", points: 1.5, description: "Responde la mayoría. Faltan detalles menores. Responde la pregunta de seguimiento." },
      { label: "Básico", points: 1.0, description: "Responde solo algunas preguntas. Faltan detalles importantes. El seguimiento es breve." },
      { label: "En proceso", points: 0.5, description: "Da respuestas muy limitadas y no cubre las partes principales de la tarea." }
    ]
  },
  {
    id: "time",
    title: "4. Tiempo de intervención",
    max: 1,
    options: [
      { label: "Excelente", points: 1.0, description: "Habla durante el tiempo estipulado y finaliza con claridad." },
      { label: "Bueno", points: 0.75, description: "Habla un poco menos o más de lo esperado, pero la respuesta está mayormente completa." },
      { label: "Básico", points: 0.5, description: "La intervención es muy corta o muy extensa. El tiempo afecta la claridad." },
      { label: "En proceso", points: 0.25, description: "Se detiene muy pronto, necesita reiniciar varias veces o no cumple el tiempo." }
    ]
  },
  {
    id: "languageTarget",
    title: "5. Vocabulario y gramática de la unidad/clase",
    max: 1.5,
    options: [
      { label: "Excelente", points: 1.5, description: "Usa vocabulario y gramática objetivo con suficiente precisión para comunicarse claramente." },
      { label: "Bueno", points: 1.0, description: "Usa parte del lenguaje objetivo. Hay errores, pero el mensaje sigue claro." },
      { label: "Básico", points: 0.5, description: "Usa lenguaje objetivo limitado. Los errores hacen el mensaje poco claro por momentos." },
      { label: "En proceso", points: 0, description: "Muestra poco o ningún uso del vocabulario/gramática objetivo." }
    ]
  },
  {
    id: "fluency",
    title: "6. Fluidez, pronunciación e inteligibilidad",
    max: 1,
    options: [
      { label: "Excelente", points: 1.0, description: "Discurso claro, con pausas naturales. La pronunciación no impide comprensión." },
      { label: "Bueno", points: 0.75, description: "Generalmente comprensible, con algunas pausas o dificultades de pronunciación." },
      { label: "Básico", points: 0.5, description: "Difícil de seguir por momentos por pausas largas, pronunciación poco clara o discurso fragmentado." },
      { label: "En proceso", points: 0.25, description: "Muy difícil de comprender; usa palabras aisladas o se bloquea varias veces." }
    ]
  },
  {
    id: "respectfulListening",
    title: "7. Escucha respetuosa y sin interrupciones",
    max: 1.5,
    options: [
      { label: "Excelente", points: 1.5, description: "Escucha con respeto. No interrumpe ni distrae la clase." },
      { label: "Bueno", points: 1.0, description: "Una interrupción o distracción." },
      { label: "Básico", points: 0.5, description: "Dos interrupciones o distracciones." },
      { label: "En proceso", points: 0, description: "Tres o más interrupciones, o una conducta irrespetuosa que afecta la clase." }
    ]
  }
];

const QUICK_NOTES = [
  "Good confidence",
  "Clear pronunciation",
  "Strong vocabulary use",
  "Answered follow-up",
  "Needs longer answers",
  "Used Spanish",
  "Read from notes",
  "Needed repetition",
  "Needs more target grammar",
  "Interrupted / distracted",
  "Excellent improvement",
  "Did not participate"
];

const STORAGE_KEY = "lv-speaking-reports-v1";
const STATUS_KEY = "lv-speaking-status-v1";

const state = {
  classId: null,
  studentNumber: null,
  scores: {},
  notes: new Set(),
  comment: "",
  activity: "Oral speaking assessment"
};

const $ = (selector) => document.querySelector(selector);
const classScreen = $("#classScreen");
const assessmentScreen = $("#assessmentScreen");
const classGrid = $("#classGrid");
const assessmentTitle = $("#assessmentTitle");
const classMeta = $("#classMeta");
const studentCount = $("#studentCount");
const studentSearch = $("#studentSearch");
const studentSelect = $("#studentSelect");
const activityInput = $("#activityInput");
const rubricList = $("#rubricList");
const quickNotes = $("#quickNotes");
const scoreValue = $("#scoreValue");
const scoreStatus = $("#scoreStatus");
const scoreRing = $("#scoreRing");
const reportPreview = $("#reportPreview");
const actionBar = $("#actionBar");
const teacherComment = $("#teacherComment");
const historyDialog = $("#historyDialog");
const historyList = $("#historyList");
const toast = $("#toast");

function init() {
  renderClasses();
  renderRubric();
  renderQuickNotes();
  bindEvents();
  updatePreview();
  saveStudentDraft();
}

function bindEvents() {
  $("#backToClasses").addEventListener("click", showClassScreen);
  $("#markExcellent").addEventListener("click", markAllExcellent);
  $("#clearNotes").addEventListener("click", clearQuickNotes);
  $("#refreshPreview").addEventListener("click", () => { updatePreview(); showToast("Preview refreshed"); });
  $("#saveReport").addEventListener("click", saveCurrentReport);
  $("#copyReport").addEventListener("click", copyCurrentReport);
  $("#exportClassCsvBar").addEventListener("click", exportClassCsv);
  $("#exportClassPdfBar").addEventListener("click", exportClassPdf);
  $("#shareWhatsapp").addEventListener("click", shareCurrentWhatsapp);
  $("#openHistory").addEventListener("click", openHistory);
  $("#closeHistory").addEventListener("click", () => historyDialog.close());
  $("#exportClassCsv").addEventListener("click", exportClassCsv);
  $("#exportClassPdf").addEventListener("click", exportClassPdf);
  $("#copyClassSummary").addEventListener("click", copyClassSummary);
  $("#exportCsv").addEventListener("click", exportAllCsv);
  $("#exportAllPdf").addEventListener("click", exportAllPdf);
  $("#clearHistory").addEventListener("click", clearHistory);

  activityInput.addEventListener("input", () => { state.activity = activityInput.value.trim() || "Oral speaking assessment"; updatePreview(); saveStudentDraft(); });
  teacherComment.addEventListener("input", () => { state.comment = teacherComment.value.trim(); updatePreview(); saveStudentDraft(); });
  $("#previousStudent").addEventListener("click", () => moveStudent(-1));
  $("#nextStudent").addEventListener("click", () => moveStudent(1));
  $("#resetCurrent").addEventListener("click", () => { clearAssessment(true); updatePreview(); saveStudentDraft(); showToast("Current student reset"); });
  studentSearch.addEventListener("input", () => {
    saveStudentDraft();
    const before = state.studentNumber;
    renderStudentOptions();
    if (state.studentNumber !== before) loadStudentDraft();
    updatePreview();
  });
  studentSelect.addEventListener("change", () => {
    saveStudentDraft();
    state.studentNumber = Number(studentSelect.value);
    loadStudentDraft();
    updatePreview();
  });
}

function getSelectedClass() {
  return CLASS_DATA.find(item => item.id === state.classId) || null;
}

function getSelectedStudent() {
  const klass = getSelectedClass();
  if (!klass) return null;
  return klass.students.find(student => student.n === state.studentNumber) || klass.students[0] || null;
}

function renderClasses() {
  classGrid.innerHTML = "";
  CLASS_DATA.forEach((klass) => {
    const doneCount = countAssessedInClass(klass);
    const button = document.createElement("button");
    button.type = "button";
    button.className = "class-card";
    button.innerHTML = `
      <h3>${escapeHtml(klass.label)}</h3>
      <p>${escapeHtml(klass.specialty)} · Paralelo ${escapeHtml(klass.section)}</p>
      <span class="class-badge">${doneCount}/${klass.students.length} graded</span>
    `;
    button.addEventListener("click", () => selectClass(klass.id));
    classGrid.appendChild(button);
  });
}

function selectClass(classId) {
  state.classId = classId;
  const klass = getSelectedClass();
  if (!klass) return;

  assessmentTitle.textContent = klass.label;
  classMeta.textContent = `${klass.course} · Tutor(a): ${klass.tutor}`;
  studentCount.textContent = `${klass.students.length} students`;
  state.studentNumber = klass.students[0]?.n || null;
  studentSearch.value = "";
  renderStudentOptions();
  clearAssessment(false);

  classScreen.classList.remove("screen-active");
  assessmentScreen.classList.add("screen-active");
  actionBar.classList.add("visible");
  window.scrollTo({ top: 0, behavior: "smooth" });
  updatePreview();
}

function showClassScreen() {
  saveStudentDraft();
  assessmentScreen.classList.remove("screen-active");
  classScreen.classList.add("screen-active");
  actionBar.classList.remove("visible");
  renderClasses();
  window.scrollTo({ top: 0, behavior: "smooth" });
}

function renderStudentOptions() {
  const klass = getSelectedClass();
  if (!klass) return;
  const query = normalize(studentSearch.value);
  const current = state.studentNumber;
  const filtered = klass.students.filter(student =>
    normalize(student.name).includes(query) || String(student.n).includes(query)
  );

  studentSelect.innerHTML = "";
  const studentsToShow = filtered.length ? filtered : klass.students;
  studentsToShow.forEach(student => {
    const option = document.createElement("option");
    option.value = String(student.n);
    option.textContent = `${student.n}. ${student.name}`;
    studentSelect.appendChild(option);
  });

  if (studentsToShow.some(student => student.n === current)) {
    studentSelect.value = String(current);
  } else if (studentsToShow[0]) {
    state.studentNumber = studentsToShow[0].n;
    studentSelect.value = String(state.studentNumber);
  }
}

function moveStudent(direction) {
  const klass = getSelectedClass();
  if (!klass || !klass.students.length) return;
  saveStudentDraft();
  const currentIndex = klass.students.findIndex(student => student.n === state.studentNumber);
  const nextIndex = (currentIndex + direction + klass.students.length) % klass.students.length;
  state.studentNumber = klass.students[nextIndex].n;
  studentSearch.value = "";
  renderStudentOptions();
  studentSelect.value = String(state.studentNumber);
  loadStudentDraft();
  updatePreview();
  window.scrollTo({ top: 0, behavior: "smooth" });
}

function renderRubric() {
  rubricList.innerHTML = "";
  RUBRIC.forEach((criterion) => {
    const card = document.createElement("article");
    card.className = "rubric-card";
    const header = document.createElement("div");
    header.className = "rubric-card-header";
    header.innerHTML = `<h4>${escapeHtml(criterion.title)}</h4><span class="max-pill">Max ${formatScore(criterion.max)}</span>`;

    const options = document.createElement("div");
    options.className = "option-grid";
    criterion.options.forEach((option, index) => {
      const button = document.createElement("button");
      button.type = "button";
      button.className = "option-button";
      button.dataset.criterion = criterion.id;
      button.dataset.points = String(option.points);
      button.dataset.index = String(index);
      button.innerHTML = `
        <span class="option-text">
          <span class="option-label"><span>${escapeHtml(option.label)}</span><span class="points-pill">${formatScore(option.points)}</span></span>
          <span class="option-description">${escapeHtml(option.description)}</span>
        </span>
      `;
      button.addEventListener("click", () => selectRubricOption(criterion.id, index));
      options.appendChild(button);
    });

    card.append(header, options);
    rubricList.appendChild(card);
  });
}

function renderQuickNotes() {
  quickNotes.innerHTML = "";
  QUICK_NOTES.forEach((note, index) => {
    const id = `note-${index}`;
    const label = document.createElement("label");
    label.className = "note-checkbox";
    label.innerHTML = `<input type="checkbox" id="${id}" value="${escapeAttribute(note)}" /> <span>${escapeHtml(note)}</span>`;
    const input = label.querySelector("input");
    input.addEventListener("change", () => {
      if (input.checked) state.notes.add(note);
      else state.notes.delete(note);
      updatePreview();
      saveStudentDraft();
    });
    quickNotes.appendChild(label);
  });
}

function selectRubricOption(criterionId, optionIndex) {
  const criterion = RUBRIC.find(item => item.id === criterionId);
  if (!criterion || !criterion.options[optionIndex]) return;
  state.scores[criterionId] = { optionIndex, points: criterion.options[optionIndex].points };
  updateRubricSelection();
  updateScore();
  updatePreview();
  saveStudentDraft();
}

function updateRubricSelection() {
  document.querySelectorAll(".option-button").forEach(button => {
    const criterionId = button.dataset.criterion;
    const selected = state.scores[criterionId];
    button.classList.toggle("selected", Boolean(selected && Number(button.dataset.index) === selected.optionIndex));
  });
}

function updateScore() {
  const marked = Object.keys(state.scores).length;
  const total = getTotalScore();
  const percentage = Math.round((total / 10) * 100);
  scoreValue.textContent = `${formatScore(total)} / 10`;
  scoreStatus.textContent = marked === RUBRIC.length ? "All criteria marked" : `${marked} of ${RUBRIC.length} criteria marked`;
  scoreRing.textContent = `${percentage}%`;
  scoreRing.style.background = `conic-gradient(var(--yellow) ${percentage * 3.6}deg, rgba(255,255,255,.23) 0deg)`;
}

function getTotalScore() {
  const total = Object.values(state.scores).reduce((sum, item) => sum + Number(item.points || 0), 0);
  return Math.round(total * 100) / 100;
}

function markAllExcellent() {
  RUBRIC.forEach(criterion => {
    state.scores[criterion.id] = { optionIndex: 0, points: criterion.options[0].points };
  });
  updateRubricSelection();
  updateScore();
  updatePreview();
  saveStudentDraft();
  showToast("All criteria marked as excellent");
}

function clearQuickNotes() {
  state.notes.clear();
  document.querySelectorAll("#quickNotes input").forEach(input => input.checked = false);
  updatePreview();
  saveStudentDraft();
}

function clearAssessment(keepStudent = true) {
  state.scores = {};
  state.notes = new Set();
  state.comment = "";
  state.activity = activityInput.value.trim() || "Oral speaking assessment";
  teacherComment.value = "";
  document.querySelectorAll("#quickNotes input").forEach(input => input.checked = false);
  updateRubricSelection();
  updateScore();
  if (!keepStudent) loadStudentDraft();
}

function getReportObject() {
  const klass = getSelectedClass();
  const student = getSelectedStudent();
  const now = new Date();
  const total = getTotalScore();
  const rows = RUBRIC.map((criterion) => {
    const selected = state.scores[criterion.id];
    const option = selected ? criterion.options[selected.optionIndex] : null;
    return {
      criterion: criterion.title,
      max: criterion.max,
      level: option ? option.label : "Not marked",
      points: selected ? selected.points : 0,
      observation: option ? option.description : ""
    };
  });

  return {
    id: `${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
    createdAt: now.toISOString(),
    date: formatDateTime(now),
    classId: klass?.id || "",
    classLabel: klass?.label || "No class selected",
    course: klass?.course || "",
    section: klass?.section || "",
    specialty: klass?.specialty || "",
    tutor: klass?.tutor || "",
    studentNumber: student?.n || "",
    studentName: student?.name || "No student selected",
    activity: state.activity || "Oral speaking assessment",
    total,
    rows,
    notes: Array.from(state.notes),
    comment: state.comment || ""
  };
}

function buildReportText(report = getReportObject()) {
  const marked = report.rows.filter(row => row.level !== "Not marked").length;
  const noteText = report.notes.length ? report.notes.map(note => `- ${note}`).join("\n") : "No quick observations selected.";
  const rubricText = report.rows.map(row =>
    `${row.criterion}
   Level: ${row.level} · Points: ${formatScore(row.points)}/${formatScore(row.max)}`
  ).join("\n");

  return [
    "SPEAKING PERFORMANCE REPORT",
    "UNIDAD EDUCATIVA PARTICULAR LEV VIGOTSKY",
    "",
    `Class: ${report.classLabel}`,
    `Student: #${report.studentNumber} - ${report.studentName}`,
    `Activity: ${report.activity}`,
    `Date: ${report.date}`,
    `Score: ${formatScore(report.total)} / 10`,
    `Rubric status: ${marked}/${RUBRIC.length} criteria marked`,
    "",
    "RUBRIC DETAILS",
    rubricText,
    "",
    "QUICK OBSERVATIONS",
    noteText,
    "",
    "TEACHER COMMENT",
    report.comment || "No additional comment.",
    "",
    "Teacher: Mr. David Santana"
  ].join("\n");
}

function updatePreview() {
  reportPreview.textContent = buildReportText();
  updateScore();
}

function saveCurrentReport() {
  const report = getReportObject();
  report.text = buildReportText(report);
  const reports = loadReports();
  reports.unshift(report);
  localStorage.setItem(STORAGE_KEY, JSON.stringify(reports.slice(0, 250)));
  markStudentSaved(report);
  showToast("Report saved locally");
  renderClasses();
  return report;
}

async function copyCurrentReport() {
  const text = buildReportText();
  try {
    await navigator.clipboard.writeText(text);
    showToast("Report copied");
  } catch (error) {
    fallbackCopy(text);
    showToast("Report copied");
  }
}

function exportCurrentTxt() {
  const report = getReportObject();
  const filename = `${safeFilename(report.classLabel)}-${safeFilename(report.studentName)}-${dateForFilename()}.txt`;
  downloadBlob(buildReportText(report), filename, "text/plain;charset=utf-8");
  showToast("TXT report exported");
}

async function shareCurrentWhatsapp() {
  const report = saveCurrentReport();
  const text = report.text || buildReportText(report);

  if (navigator.share) {
    try {
      await navigator.share({ title: "Speaking Performance Report", text });
      showToast("Share sheet opened");
      return;
    } catch (error) {
      // Fall back to WhatsApp link if sharing is cancelled or unavailable.
    }
  }

  const url = `https://wa.me/?text=${encodeURIComponent(text)}`;
  window.open(url, "_blank", "noopener,noreferrer");
  showToast("Opening WhatsApp");
}

function loadReports() {
  try {
    return JSON.parse(localStorage.getItem(STORAGE_KEY) || "[]");
  } catch (error) {
    return [];
  }
}

function loadStatus() {
  try {
    return JSON.parse(localStorage.getItem(STATUS_KEY) || "{}");
  } catch (error) {
    return {};
  }
}

function markStudentSaved(report) {
  const status = loadStatus();
  status[report.classId] = status[report.classId] || {};
  status[report.classId][report.studentNumber] = { score: report.total, date: report.date };
  localStorage.setItem(STATUS_KEY, JSON.stringify(status));
}

function saveStudentDraft() {
  const klass = getSelectedClass();
  const student = getSelectedStudent();
  if (!klass || !student) return;
  const key = `lv-draft-${klass.id}-${student.n}`;
  localStorage.setItem(key, JSON.stringify({
    scores: state.scores,
    notes: Array.from(state.notes),
    comment: state.comment,
    activity: state.activity,
    updatedAt: new Date().toISOString()
  }));
}

function loadStudentDraft() {
  const klass = getSelectedClass();
  const student = getSelectedStudent();
  if (!klass || !student) return;
  const key = `lv-draft-${klass.id}-${student.n}`;
  let draft = null;
  try { draft = JSON.parse(localStorage.getItem(key) || "null"); } catch (error) { draft = null; }

  state.scores = draft?.scores || {};
  state.notes = new Set(draft?.notes || []);
  state.comment = draft?.comment || "";
  state.activity = draft?.activity || activityInput.value.trim() || "Oral speaking assessment";
  activityInput.value = state.activity;
  teacherComment.value = state.comment;
  document.querySelectorAll("#quickNotes input").forEach(input => {
    input.checked = state.notes.has(input.value);
  });
  updateRubricSelection();
  updateScore();
}

function openHistory() {
  renderHistory();
  if (typeof historyDialog.showModal === "function") historyDialog.showModal();
  else historyDialog.setAttribute("open", "");
}

function renderHistory() {
  const reports = loadReports();
  historyList.innerHTML = "";
  if (!reports.length) {
    historyList.innerHTML = `<div class="empty-state">No saved reports yet.</div>`;
    return;
  }

  reports.forEach(report => {
    const item = document.createElement("article");
    item.className = "history-item";
    item.innerHTML = `
      <strong>${escapeHtml(report.studentName)} · ${formatScore(report.total)}/10</strong>
      <p>${escapeHtml(report.classLabel)} · ${escapeHtml(report.date)} · ${escapeHtml(report.activity)}</p>
      <div class="history-mini-actions">
        <button type="button" data-action="copy">Copy</button>
        <button type="button" data-action="txt">TXT</button>
        <button type="button" data-action="whatsapp">WhatsApp</button>
      </div>
    `;
    item.querySelector('[data-action="copy"]').addEventListener("click", async () => {
      await navigator.clipboard.writeText(report.text || buildReportText(report));
      showToast("Saved report copied");
    });
    item.querySelector('[data-action="txt"]').addEventListener("click", () => {
      const filename = `${safeFilename(report.classLabel)}-${safeFilename(report.studentName)}-${dateForFilename(new Date(report.createdAt))}.txt`;
      downloadBlob(report.text || buildReportText(report), filename, "text/plain;charset=utf-8");
    });
    item.querySelector('[data-action="whatsapp"]').addEventListener("click", () => {
      window.open(`https://wa.me/?text=${encodeURIComponent(report.text || buildReportText(report))}`, "_blank", "noopener,noreferrer");
    });
    historyList.appendChild(item);
  });
}

const RUBRIC_SHORT = [
  "English only",
  "No reading",
  "Task coverage",
  "Time",
  "Vocabulary / grammar",
  "Fluency / pronunciation",
  "Respectful listening"
];

function getDraftForStudent(classId, studentNumber) {
  try {
    return JSON.parse(localStorage.getItem(`lv-draft-${classId}-${studentNumber}`) || "null");
  } catch (error) {
    return null;
  }
}

function hasDraftData(draft) {
  if (!draft) return false;
  const scoreCount = Object.keys(draft.scores || {}).length;
  const noteCount = Array.isArray(draft.notes) ? draft.notes.length : 0;
  const comment = String(draft.comment || "").trim();
  return scoreCount > 0 || noteCount > 0 || comment.length > 0;
}

function buildLatestSavedMap(reports = loadReports()) {
  const map = new Map();
  reports.forEach(report => {
    const key = `${report.classId}|${report.studentNumber}`;
    if (!map.has(key)) map.set(key, report);
  });
  return map;
}

function buildReportFromDraft(klass, student, draft) {
  const rows = RUBRIC.map((criterion) => {
    const selected = draft?.scores?.[criterion.id];
    const option = Number.isInteger(selected?.optionIndex) ? criterion.options[selected.optionIndex] : null;
    return {
      criterion: criterion.title,
      max: criterion.max,
      level: option ? option.label : "Not marked",
      points: option ? option.points : 0,
      observation: option ? option.description : ""
    };
  });
  const total = rows.reduce((sum, row) => sum + Number(row.points || 0), 0);
  const updatedAt = draft?.updatedAt || new Date().toISOString();
  return {
    id: `draft-${klass.id}-${student.n}`,
    createdAt: updatedAt,
    date: safeDateLabel(updatedAt),
    classId: klass.id,
    classLabel: klass.label,
    course: klass.course,
    section: klass.section,
    specialty: klass.specialty,
    tutor: klass.tutor,
    studentNumber: student.n,
    studentName: student.name,
    activity: draft?.activity || "Oral speaking assessment",
    total: Math.round(total * 100) / 100,
    rows,
    notes: Array.isArray(draft?.notes) ? draft.notes : [],
    comment: draft?.comment || "",
    text: ""
  };
}

function getMarkedCount(report) {
  return (report?.rows || []).filter(row => row.level !== "Not marked").length;
}

function countAssessedInClass(klass) {
  const latestSaved = buildLatestSavedMap(loadReports().filter(report => report.classId === klass.id));
  return klass.students.reduce((count, student) => {
    const draft = getDraftForStudent(klass.id, student.n);
    const saved = latestSaved.get(`${klass.id}|${student.n}`);
    const report = hasDraftData(draft) ? buildReportFromDraft(klass, student, draft) : saved;
    return count + (getMarkedCount(report) > 0 ? 1 : 0);
  }, 0);
}

function getClassExportRows() {
  const klass = getSelectedClass();
  if (!klass) return [];
  saveStudentDraft();
  const latestSaved = buildLatestSavedMap(loadReports().filter(report => report.classId === klass.id));
  return klass.students.map(student => {
    const draft = getDraftForStudent(klass.id, student.n);
    const saved = latestSaved.get(`${klass.id}|${student.n}`);
    const report = hasDraftData(draft) ? buildReportFromDraft(klass, student, draft) : saved || null;
    const marked = getMarkedCount(report);
    return {
      klass,
      student,
      report,
      marked,
      status: marked > 0 ? "Assessed" : "Pending"
    };
  });
}

function getLatestSavedRows() {
  const latestSaved = buildLatestSavedMap(loadReports());
  return Array.from(latestSaved.values()).map(report => {
    const klass = CLASS_DATA.find(item => item.id === report.classId) || {
      id: report.classId,
      label: report.classLabel,
      course: report.course,
      section: report.section,
      specialty: report.specialty,
      tutor: report.tutor,
      students: []
    };
    const student = { n: report.studentNumber, name: report.studentName };
    const marked = getMarkedCount(report);
    return { klass, student, report, marked, status: marked > 0 ? "Assessed" : "Pending" };
  });
}

function buildGradeCsv(rows) {
  const header = [
    "Class", "Course", "Section", "Student Number", "Student", "Status", "Activity", "Date", "Total /10", "Marked Criteria",
    ...RUBRIC.flatMap(criterion => [criterion.title + " Level", criterion.title + " Points"]),
    "Quick Notes", "Teacher Comment"
  ];

  const body = rows.map(({ klass, student, report, status, marked }) => {
    const rowMap = Object.fromEntries((report?.rows || []).map(row => [row.criterion, row]));
    return [
      klass?.label || report?.classLabel || "",
      klass?.course || report?.course || "",
      klass?.section || report?.section || "",
      student?.n || report?.studentNumber || "",
      student?.name || report?.studentName || "",
      status,
      report?.activity || "",
      report?.date || "",
      status === "Assessed" ? formatScore(report?.total || 0) : "",
      marked || 0,
      ...RUBRIC.flatMap(criterion => [
        rowMap[criterion.title]?.level || "",
        rowMap[criterion.title] ? formatScore(rowMap[criterion.title].points || 0) : ""
      ]),
      (report?.notes || []).join(" | "),
      report?.comment || ""
    ];
  });

  return [header, ...body].map(row => row.map(csvCell).join(";")).join("\n");
}

function getRowsStats(rows) {
  const assessedRows = rows.filter(row => row.status === "Assessed");
  const scores = assessedRows.map(row => Number(row.report?.total || 0));
  const average = scores.length ? scores.reduce((sum, score) => sum + score, 0) / scores.length : 0;
  return {
    total: rows.length,
    assessed: assessedRows.length,
    pending: rows.length - assessedRows.length,
    average: Math.round(average * 100) / 100
  };
}

function exportClassCsv() {
  const klass = getSelectedClass();
  if (!klass) { showToast("Select a class first"); return; }
  const rows = getClassExportRows();
  const csv = buildGradeCsv(rows);
  downloadBlob("﻿" + csv, `${safeFilename(klass.label)}-speaking-grades-${dateForFilename()}.csv`, "text/csv;charset=utf-8");
  showToast("Class CSV exported");
}

function exportAllCsv() {
  const rows = getLatestSavedRows();
  if (!rows.length) { showToast("No saved grades to export"); return; }
  const csv = buildGradeCsv(rows);
  downloadBlob("﻿" + csv, `all-speaking-grades-${dateForFilename()}.csv`, "text/csv;charset=utf-8");
  showToast("All saved grades exported as CSV");
}

function exportClassPdf() {
  const klass = getSelectedClass();
  if (!klass) { showToast("Select a class first"); return; }
  const rows = getClassExportRows();
  const html = buildPrintableGradeHtml(rows, {
    title: "Speaking Performance Grade Report",
    subtitle: `${klass.label} · ${klass.course} · Paralelo ${klass.section}`,
    includePending: true
  });
  openPrintableReport(html, `${safeFilename(klass.label)}-speaking-grades-${dateForFilename()}.html`);
}

function exportAllPdf() {
  const rows = getLatestSavedRows();
  if (!rows.length) { showToast("No saved grades to export"); return; }
  const html = buildPrintableGradeHtml(rows, {
    title: "Speaking Performance Grade Report",
    subtitle: "Latest saved speaking grades from this device",
    includePending: false
  });
  openPrintableReport(html, `all-speaking-grades-${dateForFilename()}.html`);
}

async function copyClassSummary() {
  const klass = getSelectedClass();
  if (!klass) { showToast("Select a class first"); return; }
  const rows = getClassExportRows();
  const stats = getRowsStats(rows);
  const assessed = rows.filter(row => row.status === "Assessed");
  const lines = [
    "SPEAKING PERFORMANCE CLASS SUMMARY",
    `Class: ${klass.label}`,
    `Generated: ${formatDateTime(new Date())}`,
    `Assessed: ${stats.assessed}/${stats.total}`,
    `Pending: ${stats.pending}`,
    `Average: ${stats.assessed ? formatScore(stats.average) : "N/A"} / 10`,
    "",
    "GRADES"
  ];
  if (!assessed.length) lines.push("No assessed students yet.");
  assessed.forEach(({ student, report }) => {
    lines.push(`#${student.n} ${student.name}: ${formatScore(report.total)} / 10`);
  });
  const pending = rows.filter(row => row.status !== "Assessed");
  if (pending.length) {
    lines.push("", "PENDING");
    pending.forEach(({ student }) => lines.push(`#${student.n} ${student.name}`));
  }

  try {
    await navigator.clipboard.writeText(lines.join("\n"));
    showToast("Class summary copied");
  } catch (error) {
    fallbackCopy(lines.join("\n"));
    showToast("Class summary copied");
  }
}

function buildPrintableGradeHtml(rows, options = {}) {
  const title = options.title || "Speaking Performance Grade Report";
  const subtitle = options.subtitle || "";
  const stats = getRowsStats(rows);
  const generated = formatDateTime(new Date());
  const grouped = rows.reduce((map, row) => {
    const key = row.klass?.label || row.report?.classLabel || "Class";
    if (!map.has(key)) map.set(key, []);
    map.get(key).push(row);
    return map;
  }, new Map());

  const summaryCards = `
    <section class="summary-grid">
      <div><span>Total students</span><strong>${stats.total}</strong></div>
      <div><span>Assessed</span><strong>${stats.assessed}</strong></div>
      <div><span>Pending</span><strong>${stats.pending}</strong></div>
      <div><span>Average</span><strong>${stats.assessed ? `${formatScore(stats.average)} / 10` : "N/A"}</strong></div>
    </section>
  `;

  const classSections = Array.from(grouped.entries()).map(([classLabel, classRows]) => {
    const first = classRows[0] || {};
    const klass = first.klass || {};
    const mainRows = classRows.map(({ student, report, status, marked }) => `
      <tr class="${status === "Assessed" ? "" : "pending"}">
        <td class="num">${escapeHtml(student?.n || report?.studentNumber || "")}</td>
        <td><strong>${escapeHtml(student?.name || report?.studentName || "")}</strong></td>
        <td class="score">${status === "Assessed" ? escapeHtml(formatScore(report?.total || 0)) : "-"}</td>
        <td>${escapeHtml(status)}</td>
        <td>${escapeHtml(marked || 0)}/${RUBRIC.length}</td>
        <td>${escapeHtml(report?.activity || "")}</td>
        <td>${escapeHtml((report?.notes || []).join(" · "))}</td>
        <td>${escapeHtml(report?.comment || "")}</td>
      </tr>
    `).join("");

    const detailRows = classRows.filter(row => row.status === "Assessed").map(({ student, report }) => {
      const rowMap = Object.fromEntries((report?.rows || []).map(row => [row.criterion, row]));
      const cells = RUBRIC.map((criterion, index) => {
        const row = rowMap[criterion.title];
        return `<td>${row ? `${escapeHtml(row.level)}<br><b>${escapeHtml(formatScore(row.points))}/${escapeHtml(formatScore(row.max))}</b>` : "-"}</td>`;
      }).join("");
      return `
        <tr>
          <td class="num">${escapeHtml(student?.n || "")}</td>
          <td><strong>${escapeHtml(student?.name || "")}</strong></td>
          ${cells}
        </tr>
      `;
    }).join("");

    return `
      <section class="class-section">
        <h2>${escapeHtml(classLabel)}</h2>
        <p class="class-meta">${escapeHtml(klass.course || "")} ${klass.section ? `· Paralelo ${escapeHtml(klass.section)}` : ""} ${klass.tutor ? `· Tutor(a): ${escapeHtml(klass.tutor)}` : ""}</p>
        <h3>General grade table</h3>
        <table>
          <thead>
            <tr>
              <th class="num">No.</th>
              <th>Student</th>
              <th class="score">Score /10</th>
              <th>Status</th>
              <th>Rubric</th>
              <th>Activity</th>
              <th>Quick notes</th>
              <th>Teacher comment</th>
            </tr>
          </thead>
          <tbody>${mainRows}</tbody>
        </table>
        <h3>Rubric details</h3>
        ${detailRows ? `
        <table class="detail-table">
          <thead>
            <tr>
              <th class="num">No.</th>
              <th>Student</th>
              ${RUBRIC_SHORT.map(label => `<th>${escapeHtml(label)}</th>`).join("")}
            </tr>
          </thead>
          <tbody>${detailRows}</tbody>
        </table>` : `<p class="empty-note">No assessed students for rubric detail yet.</p>`}
      </section>
    `;
  }).join("");

  return `<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>${escapeHtml(title)}</title>
<style>
  @page { size: A4 landscape; margin: 10mm; }
  * { box-sizing: border-box; }
  body { margin: 0; color: #16223e; font-family: Arial, Helvetica, sans-serif; background: #f3f7ff; }
  .page { max-width: 1180px; margin: 0 auto; padding: 18px; }
  header { padding: 18px 20px; color: #fff; background: linear-gradient(135deg, #1375ff, #084bb5); border-radius: 18px; }
  .eyebrow { margin: 0 0 6px; font-size: 11px; font-weight: 800; text-transform: uppercase; letter-spacing: .08em; opacity: .84; }
  h1 { margin: 0; font-size: 30px; letter-spacing: -.04em; }
  .subtitle { margin: 8px 0 0; font-weight: 700; opacity: .92; }
  .generated { margin: 6px 0 0; font-size: 12px; opacity: .82; }
  .summary-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 10px; margin: 14px 0; }
  .summary-grid div { padding: 12px; border: 1px solid #d8e4ff; border-radius: 14px; background: #fff; }
  .summary-grid span { display: block; color: #65708a; font-size: 11px; font-weight: 800; text-transform: uppercase; letter-spacing: .06em; }
  .summary-grid strong { display: block; margin-top: 5px; font-size: 22px; }
  .class-section { margin-top: 16px; padding: 14px; border: 1px solid #d8e4ff; border-radius: 16px; background: #fff; page-break-inside: avoid; }
  h2 { margin: 0; font-size: 22px; color: #084bb5; }
  .class-meta { margin: 5px 0 12px; color: #65708a; font-size: 12px; font-weight: 700; }
  h3 { margin: 14px 0 7px; font-size: 14px; color: #17233f; }
  table { width: 100%; border-collapse: collapse; font-size: 10.5px; table-layout: fixed; }
  th, td { border: 1px solid #d7def0; padding: 6px; vertical-align: top; word-wrap: break-word; }
  th { color: #fff; background: #1375ff; font-size: 10px; text-transform: uppercase; letter-spacing: .03em; }
  tbody tr:nth-child(even) { background: #f8fbff; }
  .pending { color: #68738b; background: #fff9eb !important; }
  .num { width: 38px; text-align: center; }
  .score { width: 62px; text-align: center; font-weight: 800; }
  .detail-table th:nth-child(2), .detail-table td:nth-child(2) { width: 190px; }
  .detail-table td { font-size: 9.6px; }
  .empty-note { margin: 0; padding: 10px; border: 1px dashed #d7def0; border-radius: 10px; color: #65708a; }
  footer { margin-top: 14px; color: #65708a; font-size: 11px; text-align: center; }
  @media print {
    body { background: #fff; }
    .page { max-width: none; padding: 0; }
    header, .summary-grid div, .class-section { box-shadow: none; }
  }
</style>
</head>
<body>
  <main class="page">
    <header>
      <p class="eyebrow">Unidad Educativa Particular Lev Vigotsky · English Area</p>
      <h1>${escapeHtml(title)}</h1>
      <p class="subtitle">${escapeHtml(subtitle)}</p>
      <p class="generated">Generated: ${escapeHtml(generated)} · Teacher: Mr. David Santana</p>
    </header>
    ${summaryCards}
    ${classSections || `<p class="empty-note">No saved grades available.</p>`}
    <footer>Generated by Speaking Report Maker. Scores follow the 2026-2027 oral speaking rubric.</footer>
  </main>
</body>
</html>`;
}

function openPrintableReport(html, fallbackFilename) {
  const printWindow = window.open("", "_blank");
  if (!printWindow) {
    downloadBlob(html, fallbackFilename, "text/html;charset=utf-8");
    showToast("Popup blocked. Printable HTML exported instead.");
    return;
  }
  printWindow.document.open();
  printWindow.document.write(html);
  printWindow.document.close();
  setTimeout(() => {
    printWindow.focus();
    printWindow.print();
  }, 450);
  showToast("PDF layout opened. Choose Save as PDF.");
}

function safeDateLabel(isoDate) {
  const date = new Date(isoDate);
  if (Number.isNaN(date.getTime())) return "";
  return formatDateTime(date);
}

function clearHistory() {
  if (!confirm("Clear all saved reports from this device?")) return;
  localStorage.removeItem(STORAGE_KEY);
  localStorage.removeItem(STATUS_KEY);
  Object.keys(localStorage).forEach(key => {
    if (key.startsWith("lv-draft-")) localStorage.removeItem(key);
  });
  renderHistory();
  renderClasses();
  showToast("History cleared");
}

function fallbackCopy(text) {
  const area = document.createElement("textarea");
  area.value = text;
  area.setAttribute("readonly", "");
  area.style.position = "fixed";
  area.style.opacity = "0";
  document.body.appendChild(area);
  area.select();
  document.execCommand("copy");
  area.remove();
}

function downloadBlob(content, filename, type) {
  const blob = new Blob([content], { type });
  const url = URL.createObjectURL(blob);
  const link = document.createElement("a");
  link.href = url;
  link.download = filename;
  document.body.appendChild(link);
  link.click();
  link.remove();
  URL.revokeObjectURL(url);
}

function showToast(message) {
  toast.textContent = message;
  toast.classList.add("show");
  clearTimeout(showToast.timer);
  showToast.timer = setTimeout(() => toast.classList.remove("show"), 1800);
}

function formatScore(value) {
  const number = Number(value || 0);
  return Number.isInteger(number) ? String(number) : number.toFixed(2).replace(/0$/, "");
}

function formatDateTime(date) {
  return new Intl.DateTimeFormat("es-EC", {
    year: "numeric",
    month: "2-digit",
    day: "2-digit",
    hour: "2-digit",
    minute: "2-digit"
  }).format(date);
}

function dateForFilename(date = new Date()) {
  return date.toISOString().slice(0, 10);
}

function safeFilename(value) {
  return String(value || "report")
    .normalize("NFD")
    .replace(/[̀-ͯ]/g, "")
    .replace(/[^a-z0-9]+/gi, "-")
    .replace(/^-|-$/g, "")
    .slice(0, 60)
    .toLowerCase();
}

function csvCell(value) {
  const text = String(value ?? "").replace(/"/g, '""');
  return `"${text}"`;
}

function normalize(value) {
  return String(value || "")
    .toLowerCase()
    .normalize("NFD")
    .replace(/[̀-ͯ]/g, "")
    .trim();
}

function escapeHtml(value) {
  return String(value ?? "")
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;")
    .replace(/'/g, "&#039;");
}

function escapeAttribute(value) {
  return escapeHtml(value).replace(/`/g, "&#096;");
}

window.addEventListener("beforeunload", saveStudentDraft);
init();
